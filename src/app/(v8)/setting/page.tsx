// "use client"
// import { redirect, permanentRedirect } from "next/navigation";

export default function Page() {
      // redirect('/order')
      // permanentRedirect('/order') // 永久重定向

  return <div>
    <h1>Setting</h1>
    {/* <button onClick={() => redirect("/order")}>Go to Order</button> */}
  </div>
}